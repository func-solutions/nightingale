dependencies {
    compileOnlyApi 'cristalix:microservice:20.11.04'
}

publishing {
    publications {
        maven(MavenPublication) {
            artifactId = "nightingale-protocol"
            version project.version.toString()

            from components.java
        }
    }
}